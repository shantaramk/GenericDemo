# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GenericDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GenericDemo

  target 'GenericDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GenericDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

pod 'Firebase/RemoteConfig'
pod 'Firebase/Core'
pod 'SwiftLint'
pod 'FMDB/FTS'
end
